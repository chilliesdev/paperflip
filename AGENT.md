Never add styles manually always use tailwindcss classes.

Write automated tests for every component and function.

Components should be small and reusable.

Never create icons manually always use an icon library.
